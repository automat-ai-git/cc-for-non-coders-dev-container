# =============================================================================
# Пароль для входа в рабочую среду (IDE + File Browser)
# =============================================================================
PASSWORD=main_password

# =============================================================================
# Z.AI — провайдер по умолчанию (GLM-модели)
# Ключи: https://open.bigmodel.cn/
# =============================================================================
GLM_API_KEY=main_key
GLM_API_KEY_BACKUP=

# Модели Z.AI (раскомментируй чтобы переопределить)
# Lite-план: GLM-4.7, GLM-4.6, GLM-4.5, GLM-4.5-Air
# Pro/Max:   + GLM-5.1, GLM-5-Turbo
GLM_OPUS_MODEL=GLM-5.1
GLM_SONNET_MODEL=GLM-4.7
GLM_HAIKU_MODEL=GLM-4.5-Air

# =============================================================================
# Anthropic — реальный Claude (опционально, через LiteLLM)
# Ключи: https://console.anthropic.com/
# Модели: claude-opus-4-6 | claude-sonnet-4-6 | claude-haiku-4-5-20251001
# =============================================================================
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_OPUS_MODEL=claude-opus-4-6
# ANTHROPIC_SONNET_MODEL=claude-sonnet-4-6
# ANTHROPIC_HAIKU_MODEL=claude-haiku-4-5-20251001

# =============================================================================
# Таймаут API-запросов (мс). 3000000 = 50 минут
# =============================================================================
API_TIMEOUT_MS=3000000

# =============================================================================
# Plane MCP — интеграция с Plane (опционально)
# =============================================================================
PLANE_API_KEY=plane_api_
PLANE_WORKSPACE_SLUG=slug
PLANE_BASE_URL=http://plane-app-api-1:8000

# Роутер моделей (опционально)
# Раскомментируйте если запущен LiteLLM — тогда заработает switch-model.sh
# Без этой строки запросы идут напрямую в Z.AI, switch-model.sh local не работает
# host.docker.internal автоматически резолвится в IP хоста на любой машине
# ANTHROPIC_BASE_URL=http://host.docker.internal:4000
